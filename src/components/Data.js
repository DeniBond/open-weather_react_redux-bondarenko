import React from 'react';
import Form from "./Form";
import Weather from "./Weather";

const Data = () => {
    return (
        <div className={''}>
            <Form/>
            <Weather/>
        </div>
    );
};

export default Data;